//:include qubit/Define.js

(function () {
  var category = {
    id: 3,
    name: "Social"
  };
  
  qubit.Define.namespace(
          "qubit.opentag.data.category.Social",
          category);
}());