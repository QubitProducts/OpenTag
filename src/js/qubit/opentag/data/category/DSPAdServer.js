//:include qubit/Define.js

(function () {
  var category = {
    id: 14,
    name: "DSP (Ad Server)"
  };
  
  qubit.Define.namespace(
          "qubit.opentag.data.category.DSPAdServer",
          category);
}());