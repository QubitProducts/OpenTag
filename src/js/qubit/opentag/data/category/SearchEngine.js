//:include qubit/Define.js

(function () {
  var category = {
    id: 7,
    name: "Search Engine"
  };
  
  qubit.Define.namespace(
          "qubit.opentag.data.category.SearchEngine",
          category);
}());